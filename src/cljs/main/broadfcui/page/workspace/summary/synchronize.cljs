(ns broadfcui.page.workspace.summary.synchronize
  (:require
    [dmohs.react :as react]
    [broadfcui.utils :as utils]
    ))


(defn handle-sync [parsed-perms-report can-share?]
  (let [methods (:referencedMethods parsed-perms-report)]))
